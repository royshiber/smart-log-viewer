/** App version - increment on each change */
export const APP_VERSION = '1.02.34';

/** What's new in this version (numbered list for popup) */
export const VERSION_WHATS_NEW = {
  he: `1. Vision Landing Console (:4010): ממשק מלא במונוריפו — טאבים (מרכז פרמטרים, תחקור, תהליכים, טלמטריה, מפה טריין, יועץ), שרת SQLite + העלאות + Gemini
2. mLRS Configurator: מסך יחיד ללא גלילה, טופס פרמטרים, עוזר AI, פרוקסי /api
3. STEP Collector: CustomTkinter, Playwright, Captcha ידני, ארגון קבצים`,
  en: `1. Vision Landing Console (:4010): full UI in monorepo — tabs (parameters, debrief, processes, telemetry, terrain map, advisor), SQLite server + uploads + Gemini
2. mLRS Configurator: single viewport, parameter form, AI assistant, /api proxy
3. Desktop STEP Collector: CustomTkinter, Playwright, manual Captcha, file layout`,
};
